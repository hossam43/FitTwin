/**
 * app.js — Main entry point
 * Runs on every page; page-specific logic is in js/pages/*.js
 */

document.addEventListener('DOMContentLoaded', () => {
  // ── Global inits ──────────────────────────────────────────────────────
  initScrollReveal();
  initNavbar();
});